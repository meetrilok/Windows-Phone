<Page
    x:Class="MapControlSamples.DrivingRoutesPage"
    xmlns:Maps="using:Windows.UI.Xaml.Controls.Maps" 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MapControlSamples"
    xmlns:data="using:MapControlSamples.Data"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    >
    
    <Page.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition>
                <NavigationThemeTransition.DefaultNavigationTransitionInfo>
                    <ContinuumNavigationTransitionInfo/>
                </NavigationThemeTransition.DefaultNavigationTransitionInfo>
            </NavigationThemeTransition>
        </TransitionCollection>
    </Page.Transitions>

    <Grid x:Name="LayoutRoot">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Panel 
        <StackPanel Grid.Row="0" Margin="19,0,0,0">
            <TextBlock x:Uid="tbDrivingRoutesFrom" Text="From" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,12,0,0"/>
            <TextBlock Name="tbFrom" Style="{ThemeResource TitleTextBlockStyle}" FontWeight="Light" Margin="0,0,0,0"/>
            <TextBlock x:Uid="tbDrivingRoutesTo" Text="To" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,12,0,0"/>
            <TextBlock Name="tbTo" Style="{ThemeResource TitleTextBlockStyle}" FontWeight="Light" Margin="0,0,0,0"/>
        </StackPanel>
        -->
        <Maps:MapControl Grid.Row="1" 
            x:Name="MapwithDrivingRoute" 
            MapServiceToken="{StaticResource MapServiceTokenString}"
            Width="350"
            Height="Auto"
            Visibility="Visible"
            HorizontalAlignment="Left"
            Margin="19,9.5,19,0" 
            />

        <ScrollViewer Name="svTurnByTurn" Grid.Row="1" Visibility="Collapsed">            
            <TextBlock Name="tbTurnByTurn" 
                       Width="350"
                       Height="Auto"
                       TextWrapping="WrapWholeWords"
                       FontSize="20" 
                       Margin="19,9.5,19,0" 
                       />
        </ScrollViewer>

    </Grid>

    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton x:ConnectionId='1' x:Uid="abbMap" x:Name="abbMap" Label="Map"  Icon="Map" IsEnabled="False"                      />
            <AppBarButton x:ConnectionId='2' x:Uid="abbDirections" x:Name="abbDirections" Label="Directions"  Icon="List"                             />
        </CommandBar>
    </Page.BottomAppBar>

</Page>

