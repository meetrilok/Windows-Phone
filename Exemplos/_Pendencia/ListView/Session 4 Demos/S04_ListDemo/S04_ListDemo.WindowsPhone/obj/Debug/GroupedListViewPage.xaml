<Page
    x:Class="S04_ListDemo.GroupedListViewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:S04_ListDemo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:grouping="using:S04_ListDemo.GroupingHelpers"
    mc:Ignorable="d">
	<Page.Resources>
        <grouping:ScreenWidthMultiplier x:Key="ScreenWidth" />
        <CollectionViewSource x:Key="ItemsGroupedByGroup" 
		        IsSourceGrouped="True" 
		        ItemsPath="InternalList" 
		        Source="{Binding GroupedForZoom, Source={Binding}}"/>

		<DataTemplate x:Key="SampleDataTemplate">
			<Grid Margin="0,0,0,10">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="100" />
				<ColumnDefinition Width="*" />
			</Grid.ColumnDefinitions>
                <Border x:Name="templatePlaceholder" Background="#FF666666" Margin="1"/>
                <Image x:Name="templateImage" Source="{Binding ItemImage}" />

				<Grid Grid.Column="1" Margin="10,0,0,0">
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
					</Grid.RowDefinitions>
                        <TextBlock x:Name="templateTitle" HorizontalAlignment="Left" 
                                   Text="{Binding Title}" VerticalAlignment="Top" 
                                   Margin="0,0,0,-0.167" Style="{StaticResource ListViewItemTextBlockStyle}"/>
                        <TextBlock x:Name="templateSubTitle" HorizontalAlignment="Left" TextWrapping="Wrap" 
                                   Text="{Binding TargetGroup}" VerticalAlignment="Top" Margin="0" 
                                   Grid.Row="1" Style="{StaticResource ListViewItemSubheaderTextBlockStyle}"/>
                    </Grid>

			</Grid>
		</DataTemplate>

        <DataTemplate x:Key="UnboundSampleDataTemplate">
            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Border x:Name="templatePlaceholder" Background="#FF666666" Margin="1"/>
                <Image x:Name="templateImage"  />

                <Grid Grid.Column="1" Margin="10,0,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBlock x:Name="templateTitle" HorizontalAlignment="Left" 
                                   VerticalAlignment="Top" 
                                   Margin="0,0,0,-0.167" Style="{StaticResource ListViewItemTextBlockStyle}"/>
                    <TextBlock x:Name="templateSubTitle" HorizontalAlignment="Left" TextWrapping="Wrap" 
                                   VerticalAlignment="Top" Margin="0" 
                                   Grid.Row="1" Style="{StaticResource ListViewItemSubheaderTextBlockStyle}"/>
                </Grid>

            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="MyHeaderTemplate">
            <Grid x:Name="Office_Chair_Header" Background="{ThemeResource ButtonPressedBackgroundThemeBrush}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="23*"/>
                    <ColumnDefinition Width="22*"/>
                </Grid.ColumnDefinitions>
                <Border BorderBrush="Black" BorderThickness="0" Margin="0" Grid.Column="1">
                    <Border.Background>
                        <LinearGradientBrush EndPoint="1,0" StartPoint="0,0">
                            <GradientStop/>
                            <GradientStop Color="{ThemeResource PhoneBackgroundColor}" Offset="0.997"/>
                        </LinearGradientBrush>
                    </Border.Background>
                </Border>
                <TextBlock TextWrapping="Wrap" Text="Header Chairs" Margin="10,5,0,1" FontSize="21.333" Grid.ColumnSpan="2"/>
            </Grid>
        </DataTemplate>
        
        <DataTemplate x:Key="SampleFooterTemplate">
            <Grid x:Name="Office_Chair_Header" Background="{ThemeResource ButtonPressedBackgroundThemeBrush}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="23*"/>
                    <ColumnDefinition Width="22*"/>
                </Grid.ColumnDefinitions>
                <Border BorderBrush="Black" BorderThickness="0" Margin="0">
                    <Border.Background>
                        <LinearGradientBrush EndPoint="1,0" StartPoint="0,0">
                            <GradientStop Offset="1"/>
                            <GradientStop Color="{ThemeResource PhoneBackgroundColor}"/>
                        </LinearGradientBrush>
                    </Border.Background>
                </Border>
                <TextBlock TextWrapping="Wrap" Text="Footer Chairs" Margin="10,5" FontSize="21.333" Grid.ColumnSpan="2" HorizontalAlignment="Right"/>
            </Grid>
        </DataTemplate>
        
        <Style x:Key="Group_ItemContainerStyle" TargetType="GroupItem">
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupItem">
                        <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <ContentControl x:Name="HeaderContent" ContentTemplate="{TemplateBinding ContentTemplate}" ContentTransitions="{TemplateBinding ContentTransitions}" ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}" Content="{TemplateBinding Content}" IsTabStop="False" Margin="{TemplateBinding Padding}" TabIndex="0" HorizontalContentAlignment="Stretch"/>
                                <ItemsControl x:Name="ItemsControl" IsTabStop="False" ItemsSource="{Binding GroupItems}" Grid.Row="1" TabIndex="1" TabNavigation="Once">
                                    <ItemsControl.ItemContainerTransitions>
                                        <TransitionCollection>
                                            <AddDeleteThemeTransition/>
                                            <ReorderThemeTransition/>
                                        </TransitionCollection>
                                    </ItemsControl.ItemContainerTransitions>
                                </ItemsControl>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>


    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    	<Grid.RowDefinitions>
    		<RowDefinition Height="Auto"/>
    		<RowDefinition Height="567*"/>
    	</Grid.RowDefinitions>
        <TextBlock HorizontalAlignment="Left" 
                   TextWrapping="Wrap" 
                   Text="Grouped ListView" 
                   VerticalAlignment="Bottom" 
                   FontSize="36" 
                   FontFamily="Global User Interface" 
                   Margin="20,40,0,0"/>
        <SemanticZoom Name="semanticZoom" Margin="20,20,20,0" Grid.RowSpan="2" Grid.Row="1" >
            <SemanticZoom.ZoomedInView>

                <ListView x:ConnectionId='1' 
                    x:Name="GroupedListView"
				    ItemsSource="{Binding Source={StaticResource ItemsGroupedByGroup}}" 
				    ItemTemplate="{StaticResource SampleDataTemplate}" 
                    ContinuumNavigationTransitionInfo.ExitElementContainer="True"
                    ScrollViewer.HorizontalScrollMode="Disabled"    
                    ScrollViewer.IsHorizontalScrollChainingEnabled="False"
                    IsItemClickEnabled="True"
                    SelectionMode="None"
                                                                       
                    >
                    <!-- ContainerContentChanging="IncrementalUpdateHandler"-->
                    <ListView.GroupStyle>
                        <GroupStyle HidesIfEmpty="True" >
                            <GroupStyle.HeaderTemplate>
                                <DataTemplate>
                                    <Grid Background="{StaticResource PhoneAccentBrush}" Margin="0,0,12,5" 
                                          HorizontalAlignment="Stretch" 
                                          Width="{Binding Converter={StaticResource ScreenWidth}, ConverterParameter=1}">
                                        <TextBlock Text="{Binding Key}" Margin="12,7" FontSize="24" />
                                    </Grid>
                                </DataTemplate>
                            </GroupStyle.HeaderTemplate>
                        </GroupStyle>
                    </ListView.GroupStyle>
                </ListView>
            </SemanticZoom.ZoomedInView>
            <SemanticZoom.ZoomedOutView>
                <ListView 
                    Background="#AA000000" 
                    ItemsSource="{Binding Source={StaticResource ItemsGroupedByGroup}, Path=CollectionGroups}" 
                    HorizontalContentAlignment="Stretch" >
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Background="{StaticResource PhoneAccentBrush}" Margin="12,0,12,12" 
                                  HorizontalAlignment="Stretch"
                                  Width="{Binding Converter={StaticResource ScreenWidth}, ConverterParameter=1}"
                                  >
                                <TextBlock Text="{Binding Group.Key}" Margin="12,7" FontSize="24" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </SemanticZoom.ZoomedOutView>
        </SemanticZoom>
        

    </Grid>
    
</Page>

