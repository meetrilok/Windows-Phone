<Page
    x:Class="FlipView_03.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FlipView_03"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid 
        Background="#64B5F6" 
        x:Name="Output" 
        HorizontalAlignment="Left" 
        VerticalAlignment="Top" 
        Grid.Row="1" 
        Margin="0,0.333,0,-0.333" 
        Width="400" 
        Height="640">

        <TextBlock 
            Text="Apresentação Vertical das Fotos"             
            HorizontalAlignment="Center" 
            VerticalAlignment="Center"             
            TextWrapping="Wrap" 
            FontSize="24" 
            TextAlignment="Center"
            Width="400" 
            Margin="0,63,0,548" />

        <FlipView 
            x:Name="MeuFlipView" 
            BorderBrush="Black" 
            BorderThickness="1"
            Margin="-1,191,1,190">
            <FlipView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Image 
                            Width="400" 
                            Height="270"
                            Source="{Binding Image}" 
                            Stretch="UniformToFill"/>
                        <Border 
                            Background="#A5000000" 
                            Height="80" 
                            VerticalAlignment="Bottom">
                            <TextBlock 
                                Text="{Binding Title}"
                                FontFamily="Segoe UI" 
                                FontSize="26.667" 
                                Foreground="#CCFFFFFF" 
                                Padding="15,20"/>
                        </Border>
                    </Grid>
                </DataTemplate>
            </FlipView.ItemTemplate>

            <FlipView.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel 
                        Orientation="Vertical"/>
                </ItemsPanelTemplate>
            </FlipView.ItemsPanel>

        </FlipView>
    </Grid>

</Page>

